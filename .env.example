# API origin (no trailing slash). May use http or https; on the same hostname as this
# app (e.g. localhost), the client automatically matches the page protocol so http and https both work.
VITE_API_URL=http://localhost:5000
# Vercel: set to your API origin, e.g. https://your-backend.vercel.app
